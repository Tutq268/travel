const initialState = {
    listWord: null,
    userWork : []
}

const WordReducer = (state = initialState,action) =>{
    switch(action.type){
        default:
            return state
    }
}

export default WordReducer